Реализовать форматированный рекурсивный вывод.
Путь к папке:
reed.pcb
врвпр.txt
А без вывода папок - просто список файлов выводить.

Добавить возможность не выводить результат на консоль, а только в файл (опция: -h).

Добавить возможность не выводить в файл и на консоль список директорий. Опция: "-d"  - означает выводить список директорий.

Добавить возможность рекурсивно выводить файлы и папки. Опция: "-r" - означает выводить список всех файлов в текущей директории
и файлов во всех нижних директориях, НО не выводит список директорий, в которых находятся файлы.

Опция: "-rd" - означает рекурсивно выводить список директорий в дополнении со списком файлов.

Добавить возможность добавлять опции (--help, -r, -h, -d) в любом месте.

Добавить возможность объединять опции: -d, -h, -r, -dh, -dr, -hd, -hr, -rd, -rh, -dhr, -drh, -hdr, -hrd, -rdh, -rhd.
Реализовать проверку на правильность ввода опций и адресов.

Переписать справку в соответствии с новыми опциями.

Переписать программу с использование классов.

Записать программу в переменные окружения, чтобы можно было с любого места использовать её.
PATH=E:\BulSV\Qt\!!!DLL;
DirList E:\BulSV\Qt\!!!DLL\DirList.exe

Добавить фильтр: какие файлы и/или папки выводить или нет (возможно по расширению и/или по имени).
Возможная опция (маска): -m:"*.cpp,*.h,*.bak,*.log,3D,?rash,?правка,?emp".

Добавить опцию на вывод только относительных или только абсолютных путей.
Возможная опция: -a.

Добавить опцию на показ или не показ скрытых файлов и папок.
Возможная опция (системные): -s.
